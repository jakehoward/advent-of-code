import pytest

from utils.path_finding import solve_graph_for_single_node # shortest_path, all_shortest_paths, all_shortest_paths_to_end, shortest_path_cost


class TestDijkstra:
    def test_shortest_paths(self):
        # assert (all_shortest_paths(graph, get_nbrs, get_cost, start) == 5)
        pass

